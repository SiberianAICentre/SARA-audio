"""Generate the pending-feature reference directly from registry and code."""

from __future__ import annotations

import ast
import json
from collections import defaultdict
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SOURCES = {
    "STR": "Из последовательности высказываний и контекста разговора. Нужен смысловой анализ по экспертным критериям: тематическая связность, аргументы, ход рассуждения. Возможный исполнитель: эксперт или проверенная на экспертной разметке модель. Подсчет отдельных слов этого не заменяет.",
    "LEX": "Из слов, выражений и их контекста в расшифровке. Нужны лемматизация, словари отношений между словами и контекстный анализ. Речевые ошибки и пропуски нужно сверять с аудио, чтобы не принять ошибку ASR за особенность говорящего.",
    "MOR": "Из грамматических форм слов в расшифровке: залога, наклонения, части речи и контекстной функции. Нужны морфологический разбор и правила, для ряда признаков также синтаксический анализ. Для слов «часто», «много» и «преобладание» нужно определить знаменатель и пороги.",
    "ORT": "Из исходного письменного текста, написанного самим исследуемым человеком. ASR, ручная расшифровка аудио и редактура ChatGPT выбирают написание за говорящего. На таком тексте можно оценивать написание расшифровки, но нельзя приписывать орфографические ошибки говорящему. Для данного аудиокорпуса эти признаки как характеристики автора требуют отдельного письменного материала.",
    "PUN": "Из пунктуации и синтаксической структуры письменного текста. В расшифровке знаки расставляет ASR или редактор, поэтому результат характеризует эту версию текста. Если нужна пунктуационная грамотность говорящего, необходим его собственный письменный текст; анализ синтаксиса устной речи следует задавать отдельно.",
    "SYN": "Из структуры предложений и связей слов в расшифровке. Нужны синтаксический разбор, морфологические признаки и правила; для неполных предложений и выбора альтернативных конструкций нужен контекст. Границы предложений ASR и склейки разных говорящих необходимо проверять.",
    "EMO": "Из оценочной лексики, словообразования и смысла высказываний. Суффиксы можно искать морфологически, типы оценки требуют контекстной классификации и экспертных критериев. Это текстовые признаки выраженной оценки, а не измерение эмоций по голосу или достоверное определение внутреннего состояния человека.",
}


def main() -> None:
    registry = json.loads(
        (ROOT / "src/sara_audio/data/linguistic_features.json").read_text(encoding="utf-8")
    )
    tree = ast.parse((ROOT / "src/sara_audio/linguistic.py").read_text(encoding="utf-8"))
    rules = next(
        node.value
        for node in ast.walk(tree)
        if isinstance(node, ast.AnnAssign)
        and isinstance(node.target, ast.Attribute)
        and node.target.attr == "_rules"
    )
    implemented = {node.value for node in rules.keys}
    groups = defaultdict(list)
    for feature in registry["features"]:
        if feature["code"] not in implemented:
            groups[feature["code"].split("_", 1)[0]].append(feature)
    count = sum(len(features) for features in groups.values())
    assert len(registry["features"]) == 92
    assert count == 73
    lines = [
        "# 73 лингвистических признака без реализации расчета",
        "",
        "Сверено 10.09.2026 с реестром `src/sara_audio/data/linguistic_features.json` и правилами `src/sara_audio/linguistic.py`. Источник определений: предоставленный документ «92 параметра.docx». Все коды и формулировки ниже перенесены скриптом без ручного переписывания.",
        "",
        "Сейчас эти признаки не извлекаются: в результатах при наличии текста сохраняются строки с пустым значением и `source=not-computed`. Описанные ниже методы являются необходимыми доработками, а не уже подключенными функциями. Остальные 19 признаков имеют простые правила расчета, качество которых также требует проверки.",
        "",
        "openSMILE рассчитывает отдельный набор акустических признаков. Более мощный Whisper улучшает входную расшифровку, но сам по себе не добавляет вычисление этих 73 признаков.",
        "",
        "| Группа | Не реализовано | Всего в реестре |",
        "|---|---:|---:|",
    ]
    for prefix, features in groups.items():
        total = sum(f["code"].startswith(prefix + "_") for f in registry["features"])
        lines.append(f"| {features[0]['group_title']} ({prefix}) | {len(features)} | {total} |")
    for prefix, features in groups.items():
        lines.extend(["", f"## {features[0]['group_title']}: {len(features)}", "", SOURCES[prefix], "", "| Код | Исходное определение |", "|---|---|"])
        for feature in features:
            lines.append(f"| `{feature['code']}` | {feature['description']} |")
    lines.extend([
        "", "## Что уточнить в определениях", "",
        "- `MOR_GenderErr`: формулировка «Отсутствие единства выпадения пола автора» сохранена буквально из реестра. Она неоднозначна; до реализации нужно уточнение экспертов, а не догадка разработчика.",
        "- Слова «часто», «много», «неоправданное», «логические ошибки» требуют согласованных критериев и примеров. Не для всех признаков корректна нынешняя общая единица `count`: могут понадобиться доли, бинарные признаки или шкала оценки.",
        "- Отсутствие найденного примера не всегда доказывает противоположный признак. Например, нельзя автоматически считать текст логически корректным, если обработчик не обнаружил ошибку.",
        "- Для разговора нужно фиксировать область анализа: оператор, клиент или диалог. Реплики разных сторон нельзя смешивать при оценке признаков одного человека.",
        "", "## Применение к новым данным", "",
        "`df_full.text` подходит как отдельный источник для воспроизведения анализа НМ. Обработанные TXT дают другую версию материала: полный диалог с назначенными ролями. Выбор источника должен сохраняться в каждом результате. Метки времени, названия ролей и зарегистрированные вставки «Тишина» не являются словами говорящего.",
        "",
        "Практический порядок реализации: морфология и синтаксис, затем лексические и словообразовательные правила, затем смысловые признаки с экспертной проверкой. ORT и авторская пунктуация требуют решения о применимости к устной речи и отдельном письменном материале. Все 92 кода остаются в реестре независимо от применимости и наличия расчета.",
    ])
    destination = ROOT / "PENDING_73_FEATURES.md"
    destination.write_text("\n".join(lines) + "\n", encoding="utf-8")
    print(f"Generated {destination.name}: {count} definitions, {len(groups)} groups.")


if __name__ == "__main__":
    main()
