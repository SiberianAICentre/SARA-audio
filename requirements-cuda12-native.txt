# Reproducible Windows native profile for RTX 4090.
# ASR uses CTranslate2 on CUDA; PyTorch is installed CPU-only by the installer
# so its CUDA DLLs cannot conflict with the CTranslate2 runtime.
-e .[nlp,parquet,web]
setuptools==80.9.0
faster-whisper==1.2.1
# CUDA 12 + cuDNN 8 compatibility profile for faster-whisper.
ctranslate2==4.4.0
av==18.1.0
onnxruntime==1.30.0
tokenizers==0.22.2
transformers==4.57.6
huggingface-hub==0.36.2
# Required by torch 2.5.1; the CPU wheel is installed with --no-deps below.
sympy==1.13.1
